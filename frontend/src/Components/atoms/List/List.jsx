import styled from "styled-components";

const List = styled.ul`
  list-style: none;
  width: 90%; /* Removed quotes from numerical values */
  margin: 1rem auto; /* Removed quotes from numerical values */
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  padding: 0;
`;

export default List;
