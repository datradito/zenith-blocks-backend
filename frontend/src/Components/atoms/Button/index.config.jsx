const config = {
    buttonConfig: {
        label: "Create budget",
        variant: "contained",
        type: "submit",
        onClick: "handleBudgetCreateOnClick",
        innerText: "Create Budget"
    }
};

export default config