import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    boxStyle: {
        width: '95%',
        margin: '2rem auto',
        textAlign: "left",
    },
    
}));

export default useStyles;