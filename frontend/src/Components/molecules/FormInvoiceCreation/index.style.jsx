import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    boxStyle: {
        width: '95%',
        margin: '0 auto',
        textAlign: "left",
    },

}));

export default useStyles;